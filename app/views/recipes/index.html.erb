<h1>Recipes found for: <%= @search_param %></h1>

<div id="search-field-container">
  <form>
    <input id="search-field" type="search" name="search" placeholder="Search ingredient..." required="required"/>
    <input id="search-field-button" type="submit" value="Search"/>
  </form>
</div>

<div id="container">
  <table class="col_ld_12 col_md_12 col_sd_12">
    <% if @recipes.nil? || @recipes.empty? %>
        <p>No recipes found for <%= @search_param %>!</p>
    <% else %>
      <tr>
        <th scope="col">Thumbnail</th>
        <th scope="col">Title</th>
        <th scope="col">Rank</th>
      </tr>
      <% @recipes.each do |recipe| %>
          <tr class=<%= cycle('even', 'odd') %>>
            <td><%= link_to(image_tag(recipe["image_url"], height: '100', width: '100'), recipe["f2f_url"], {}) %></td>
            <td><%= link_to(sanitize(recipe["title"]), recipe["f2f_url"], {}) %></td>
            <td><%= recipe["social_rank"] %></td>
          </tr>
      <% end %>
    <% end %>
  </table>
</div>